@model IEnumerable<Source.Models.DBF.Tbservicedetail>

@{
    ViewData["Title"] = "DichVuChiTiet";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>DichVuChiTiet @ViewBag.ABC</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ServicedetailTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Position)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ServicedetailTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ServicedetailId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ServicedetailId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ServicedetailId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
