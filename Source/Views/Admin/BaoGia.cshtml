@model IEnumerable<Source.Models.DBF.Tbbaogia>

@{
    ViewData["Title"] = "BaoGia";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>BaoGia</h2>

<p>
    <a asp-action="BaoGiaAdd">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BaogiaTitle1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Position)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BaogiaTitle1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                <a asp-action="BaoGiaEdit" asp-route-id="@item.BaogiaId">Edit</a> |
                <a asp-action="BaoGiaDelete" asp-route-id="@item.BaogiaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
